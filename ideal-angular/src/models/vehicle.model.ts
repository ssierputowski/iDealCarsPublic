export interface Vehicle {
  vehicleId: string;
  vehicleYear: number;
  vehicleMake: string;
  vehicleModel: string;
  vehicleColor: string;
  vehicleCondition: string;
  vehicleDetails: string;
  vehiclePrice: number;
  vehicleImage: string;
}
